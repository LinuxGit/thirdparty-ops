---

# The Playbook of dm

- name: check config locally
  hosts: localhost
  any_errors_fatal: true
  tags:
    - always
  roles:
    - check_config_static

- name: deploying dm-worker
  hosts: dm-worker_servers
  tags:
    - dm-worker
  roles:
    - dm-worker
